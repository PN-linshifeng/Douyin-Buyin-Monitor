# 表格下面放两个统计

## 直播人均出单数：data.model.content_data.calculate_data.live_sales - data.model.content_data.calculate_data.live_match_order_num = 值，如：

直播人均出单数：33 - 2 = 31

## 直播出单规格：F3 - data.model.product.product_price.price_label.price = 值 ，如果值大于等于4，绿色字显示“高”，否则红色字显示“低”，直播平均客单价 - 到手价格，如：

直播出单规格：12 - 2 = 6 （高）

data.model.product.product_price.price_label.price 的值来自 新增接口请求，url和body 参数规则和以前一样
https://buyin.jinritemai.com/pc/selection/decision/pack_detail?ewid

body参数：{
    "scene_info": {
        "request_page": 2
    },
    "biz_id": "3628901032857330848", // originalBodyObj.biz_id
    "biz_id_type": 2, // originalBodyObj.biz_id_type
# 任务进度 (由 EXECUTE 模式在每步完成后追加)
*   [2025-12-16]
    *   步骤：1, 2, 3
    *   修改：
        *   `content.js`: 重新实现了 `fetchProductData`，根据用户提供的 Body 结构（`pc-non-core` 等）和 URL 规则（`pack_detail?ewid`）主动发起 POST 请求。更新了 `calculateStats` 和 `showPopup` 以接收并使用 `productData` 计算“直播出单规格”。修复了之前撤回操作导致的代码片段丢失问题。
        *   `injected.js`: 确认已撤回所有修改，恢复到硬编码 `POST` 和无响应拦截的状态（因 `pack_detail` 请求由 `content.js` 主动发起并处理 Promise 结果，无需 `injected.js` 广播）。
    *   更改摘要：实现了基于特定 Body 参数的商品数据主动获取功能，用于计算直播出单规格。
    *   原因：执行 Requirement 2 的数据获取修正。
    *   阻碍：无。
    *   状态：待确认biz_id_type
    "enter_from": "pc.selection_square.recommend_main",
    "data_module": "pc-non-core",
    "extra": {
        "use_kol_product": "1"
    }
}